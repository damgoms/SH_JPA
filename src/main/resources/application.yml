spring:
  datasource:
    #url: jdbc:h2:tcp://localhost/~/jpashop
    url: jdbc:mysql://database-1.cfe0ik88q1ep.ap-northeast-2.rds.amazonaws.com:3306/shDB_DEV?serverTimezone=Asia/Seoul&useSSL=false&allowPublicKeyRetrieval=true
    username: admin
    password: aa343466
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        #default_batch_fetch_size: 100
      #        show_sql: true
      format_sql: true
      dialect: org.hibernate.dialect.MySQL8Dialect
    open-in-view: false


logging.level:
  org.hibernate.SQL: debug
  org.hibernate.orm.jdbc.bind: trace